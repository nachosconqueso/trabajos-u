Proceso sin_titulo
	escribir "ingrese radio"
	leer r
	escribir "ingrese altura"
	leer h
	multi1 <- 2*pi*r*h
	multi2 <- 2*pi*r*h+2*pi*r^2
	multi3 <- pi*r^2*h
	trunc1 <- trunc (multi2)
	trunc2 <- trunc (multi3)
	Escribir "area:" trunc2
	Escribir "perimetro:" trunc2
FinProceso
